# Work Order Schedule Timeline

Angular standalone timeline UI for planning work orders across manufacturing work centers.

## Run Locally

```bash
npm install
ng serve
```

Open `http://localhost:4200`.

Stress mode (50 work centers / 10,000 orders):

```bash
http://localhost:4200/?stress
```

Also works with `http://localhost:4200/?stress=1`.

Pre-submit clean check:

```bash
rm -rf node_modules .angular dist
npm ci
npm run build
npm start
```

## Libraries Used

- `@ng-select/ng-select`: status picker in the create/edit panel with a compact selectable dropdown UX.
- `@ng-bootstrap/ng-bootstrap`: start/end date pickers (`ngb-datepicker`) for fast date selection without custom calendar logic.

## Features Implemented

- Timeline grid with fixed work-center column and horizontally scrollable date area.
- Timescale switcher for `Month` (default), `Week`, and `Day`.
- Current-day indicator rendered across the timeline.
- Work-order bars with status badges and per-bar `Edit/Delete` menu.
- Create panel (click empty timeline row) with prefilled start date and end date (+7 days).
- Edit panel (kebab `Edit`) with pre-populated values and `Save` action.
- Overlap validation on create/edit within the same work center.
- Click-outside and `Escape` handling for menu/panel dismissal.
- Stress dataset mode via query param (`?stress`) with 50 work centers and 10,000 work orders.

## Demo Video (Loom)

- Loom walkthrough (5-10 min): `ADD_LINK_HERE`
- Covers: zoom switching, create, edit, delete, overlap error, and code structure walkthrough.

## Data Model

Documents follow the technical test structure:

- `WorkCenterDocument`: `docId`, `docType: 'workCenter'`, `data.name`
- `WorkOrderDocument`: `docId`, `docType: 'workOrder'`, `data.{name, workCenterId, status, startDate, endDate}`

Sample dataset includes:

- 5 manufacturing work centers
- 9 work orders across all 4 statuses
- Multiple non-overlapping work orders on the same work center

## Architecture Notes

- Single feature component: `TimelineComponent` contains timeline rendering, interaction handlers, and form logic.
- Time columns are generated by timescale (`day | week | month`) into `visibleColumns` with `startUtcDay/endUtcDay` bounds.
- Work-order bars are positioned from date math (`utcDayToPx`) and sized by start/end span.
- Click-to-create uses inverse math (`pxToUtcDay`) to convert row click position into `YYYY-MM-DD`.
- Create/Edit panel uses a reactive form storing ISO dates (`startDate`, `endDate`) with datepicker conversion helpers.
- Overlap validation checks only work orders in the same work center and skips the currently edited order.

## AI Prompt Log

Prompt/decision notes are tracked in:

- `docs/ai-prompts.md`
